# Required in GitHub Codespaces for the remote backend URL.
# Define this value in .env.local before starting the frontend there.
VITE_CODESPACE_NAME=your-codespace-name

# When omitted, the frontend safely uses http://localhost:8000/api.